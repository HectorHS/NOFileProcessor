to set up, first set up virtual environment. then navigate to the project folder and on command line type: python -m pip install -e .

open command prompt in folder
workon clickenv
something like 
processor death-bars-2017.csv death-bars